# Student Responsibility Table

**Student ID:** 220901797
**Name Surname:** Talib YEŞİLDAL

I was responsible for implementing and analyzing Regularization Techniques to prevent overfitting on the Fashion-MNIST dataset.

Key Deliverables:

Overfitting Analysis: Identified the "Generalization Gap" in an overparameterized MLP using a baseline model with no constraints.

L2 Regularization: Integrated weight_decay into the SGD optimizer to penalize large weights, effectively reducing the divergence between training and validation loss.

Dropout Implementation: Added nn.Dropout(p=0.5) layers between hidden units. This forced the network to learn redundant representations, further closing the gap.

Early Stopping: Developed a patience-based monitoring system (patience=5) that tracked Val Loss and ensured the best model state was saved (best_model.pt), preventing training from entering the overfitting zone.

Final Outcome: The combination of these techniques improved the model's ability to generalize to unseen data, maintaining a stable Validation Loss of ~0.30 across 50 epochs.